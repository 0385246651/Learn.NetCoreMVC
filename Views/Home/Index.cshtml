@{
    ViewData["Title"] = "Home Page";

    @* Url.Action(); *@
    @* Url.ActionLink(); *@

    @* Url.RouteUrl(); *@
    @* Url.Link(); *@
}

<div class="text-center">
    <h1 class="display-4">Welcome</h1>
    <p>Learn about <a href="https://learn.microsoft.com/aspnet/core">building Web apps with ASP.NET Core</a>.</p>
</div>


<p>
    @Url.ActionLink()
</p>

<p>
    @Url.Action()
</p>


<p>
    @Url.Action("Privacy")
</p>

<p>
    @Url.Action("Privacy", new { id = 1, name = "abc" })
</p>

<p>
    @Url.Action("Jupiter", "Planet")
</p>

<p>
    @Url.Action("Jupiter", "Planet", new { id = 3 })
</p>

<p>
    @Url.Action("Index", "Product", new { area = "ProductManage", id = "123" })
</p>

<p>
    @Url.RouteUrl("default", new { action = "HelloView", controller = "Home", id = "123" })
</p>

@{
    var routeValues = new Dictionary<string, string>(){

{ "id", "123" },
{ "name", "abc" },
{ "page", "1" }
};
}

<a asp-action="Index" asp-controller="Product" asp-route="" asp-route-id="" asp-all-route-data="@routeValues"
    asp-area="ProductManage">Ví dụ</a>