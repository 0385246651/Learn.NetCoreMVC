@model List<ProductModel>

<h1>Product List</h1>

<div class="list-group">
  @foreach (var product in Model)
  {
    <a asp-action="Detail" asp-route-id="@product.Id" class="list-group-item list-group-item-action">
      @product.Name - @product.Price.ToString("c")
    </a>

  }
</div>